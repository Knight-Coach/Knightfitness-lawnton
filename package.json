{
  "name": "knightfitness-lawnton",
  "version": "1.0.0",
  "private": true,
  "description": "Knight Fitness Lawnton static site (dist/) — no build step. `npm test` runs the 5RM Board logic tests.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "npx --yes serve dist"
  },
  "engines": {
    "node": ">=18"
  }
}
